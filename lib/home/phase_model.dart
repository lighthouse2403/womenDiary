import 'package:flutter/material.dart';

class PhaseModel {
  final String emoji;
  final int days;
  final Color color;
  final int startDay;

  PhaseModel(this.emoji, this.days, this.color, this.startDay);
}